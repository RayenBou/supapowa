{% block title %}
	{{parent()}}
	Votre commande

{% endblock %}

{% block body %}


	{{order.orderId}}

	{{order.createdAt}}

	{% for product in order.product %}
		{{product.id}}
		{{product.name}}
		{{product.price}}

	{% endfor %}
	{{order.total}}


{% endblock %}
